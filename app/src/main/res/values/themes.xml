<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NewsApp2" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">
            shortEdges <!-- default, shortEdges, or never -->
        </item>
        <item name="android:textColor">@color/black</item>
        <item name="checkMarkTint">@color/black</item>
        <item name="rangeFillColor">@color/red</item>
    </style>

    <style name="FAB" parent="Theme.NewsApp2">
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <style name="Tint" parent="Theme.NewsApp2">
        <item name="tint">@color/black</item>
        <item name="drawableTint">@color/black</item>
    </style>

    <style name="foregroundGradient" parent="Theme.NewsApp2">
        <item name="android:foreground">@drawable/gradient</item>
    </style>

    <style name="TaskBarTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/accent_light</item>
        <item name="tabBackground">@color/white</item>
        <item name="tabSelectedTextColor">@color/accent_light</item>
    </style>

    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/accent_light</item>
        <item name="tabSelectedTextColor">@color/accent_light</item>
    </style>

    <style name="BackgroundTextView" parent="Theme.NewsApp2">
        <item name="android:background">@color/gray</item>
    </style>

    <style name="MyDatePickerStyle" parent="ThemeOverlay.MaterialComponents.MaterialCalendar.Fullscreen">
        <item name="colorPrimary">@color/button_light</item>
    </style>
</resources>