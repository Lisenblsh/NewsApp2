<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NewsApp2" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" targetApi="l">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">
            shortEdges <!-- default, shortEdges, or never -->
        </item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/white</item>
        <item name="checkMarkTint">@color/white</item>
    </style>

    <style name="FAB" parent="Theme.NewsApp2">
        <item name="android:backgroundTint">@color/darkGray</item>
    </style>

    <style name="Tint" parent="Theme.NewsApp2">
        <item name="tint">@color/white</item>
        <item name="drawableTint">@color/white</item>
    </style>

    <style name="foregroundGradient" parent="Theme.NewsApp2">
        <item name="android:foreground">@drawable/gradient2</item>
    </style>
    <style name="TaskBarTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/accent_night</item>
        <item name="tabBackground">@color/taskBar_night</item>
        <item name="tabSelectedTextColor">@color/accent_night</item>
    </style>
    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/accent_night</item>
        <item name="tabSelectedTextColor">@color/accent_night</item>
    </style>
    <style name="BackgroundTextView" parent="Theme.NewsApp2">
        <item name="android:background">@color/darkGray</item>
    </style>

    <style name="MyDatePickerStyle" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="colorAccent">@color/accent_night</item>
        <item name="colorControlActivated">@color/accent_night</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:windowBackground">@color/background_night</item>
    </style>
</resources>